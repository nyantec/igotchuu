[build-system]
requires = ["setuptools >= 61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "igotchuu"
version = "0.1.0"
dependencies = [
    "pygobject",
    "click",
    "python-unshare",
    "btrfsutil",
]

[project.scripts]
igotchuu = "igotchuu:cli"
